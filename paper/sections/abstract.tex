\begin{abstract}
We present WorldX, an end-to-end pipeline that transforms natural-language descriptions into executable agent worlds. Unlike approaches that assume pre-built environments, WorldX focuses on structured world planning, multimodal asset generation, grounding, and runtime configuration synthesis. The downstream agent simulator is treated as an execution probe rather than the primary contribution.
\todo{Add experimental results.}
\end{abstract}
